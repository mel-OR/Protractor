name: Test triggers workflow

on: 
  repository_dispatch:
    types: on-demand-test

jobs:
  runjob:
    name: Hello
    runs-on: ubuntu-latest
    steps:
      - name: Echo hello
        run: echo 'Hello'